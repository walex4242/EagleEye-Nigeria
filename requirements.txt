# ─────────────────────────────────────────────
# EagleEye-Nigeria — Python Dependencies
# Python 3.11+ compatible
# Install with: pip install -r requirements.txt
# ─────────────────────────────────────────────

# ── PyTorch CPU-only (saves ~2GB) ─────────────
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.3.0
torchvision>=0.18.0

# ── API Framework ─────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-dotenv>=1.0.1
pydantic>=2.7.1
python-multipart>=0.0.9

# ── Data Processing ───────────────────────────
numpy>=1.26.0
pandas>=2.0.0
requests>=2.32.0
httpx>=0.27.0

# ── AI / Computer Vision ──────────────────────
Pillow>=10.3.0
scikit-learn>=1.5.0
opencv-python-headless>=4.9.0.80

# ── Geospatial (core — no GDAL required) ─────
shapely>=2.0.6
pyproj>=3.6.0
folium>=0.16.0

# ── Geospatial (optional — need system GDAL) ──
# Uncomment these if deploying with Docker or
# on a system with GDAL installed:
# geopandas>=1.0.0
# rasterio>=1.3.10

# ── Google Earth Engine (optional) ────────────
# Uncomment if you have GEE credentials configured:
# earthengine-api>=0.1.401

# ── Database (event logging) ──────────────────
sqlalchemy>=2.0.30